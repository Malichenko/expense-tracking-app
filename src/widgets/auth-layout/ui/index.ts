import { AuthFooterLink as Footer } from "./auth-footer-link";
import { AuthHeader as Header } from "./auth-header";
import { AuthLayout as Root } from "./auth-layout";

export const AuthLayout = {
  Root,
  Header,
  Footer,
};
